{
  "private": true,
  "workspaces": [
    "examples/*",
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/core && yarn build",
    "publish": "cd packages/core && yarn publish"
  },
  "dependencies": {
    "tsup": "^7.2.0",
    "typescript": "^5.2.2"
  }
}
